name: "CI CD pipeline / workflows"

on:
 push:
    branches:
       - master
       - main   

jobs:
    buil-and-test:
        runs-on: ubuntu-latest

        steps:
            - name: "setup node js"
             uses: actions/setup-node@v4



